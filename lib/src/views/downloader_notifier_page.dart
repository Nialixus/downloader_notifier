part of '../../downloader_notifier.dart';

class DownloaderNotifierPage extends StatelessWidget {
  const DownloaderNotifierPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
