part of '../../downloader_toast.dart';

class DownloaderToastPage extends StatelessWidget {
  const DownloaderToastPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Theme.of(context).colorScheme.surface,
    );
  }
}
